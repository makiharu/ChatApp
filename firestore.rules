rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read；
      allow write: if request.auth != null;
          request.time < timestamp.date(2021, 3, 29);
    }
  }
}